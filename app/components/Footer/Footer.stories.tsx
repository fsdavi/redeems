import Footer from ".";

const meta = {
  title: "Components/Footer",
  component: Footer,
  parameters: {
    layout: "centered",
  },
}

export default meta;

const Template = () => (
  <Footer />
)

export const Default = Template.bind({});